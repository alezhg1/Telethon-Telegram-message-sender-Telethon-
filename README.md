# 📱 Telegram Message Sender - Консольная версия

> **Приложение для отправки сообщений в Telegram** 🚀 НЕ ИСПОЛЬЗОВАТЬ ДЛЯ СПАМА

[![Python 3.8+](https://img.shields.io/badge/python-3.8%2B-blue)](https://www.python.org/downloads/)
[![Telethon](https://img.shields.io/badge/telethon-1.26%2B-green)](https://github.com/LonamiWebs/Telethon)
[![MIT License](https://img.shields.io/badge/license-MIT-purple)](LICENSE)
[![Status](https://img.shields.io/badge/status-active-brightgreen)](https://github.com)

---

## 📋 Содержание

- [Возможности](#-возможности)
- [Требования](#-требования)
- [Установка](#-установка)
- [Конфигурация](#-конфигурация)
- [Использование](#-использование)
- [Структура проекта](#-структура-проекта)
- [API и классы](#-api-и-классы)
- [Примеры использования](#-примеры-использования)
- [Обработка ошибок](#-обработка-ошибок)
- [Советы и трюки](#-советы-и-трюки)
- [Лицензия](#-лицензия)

---

## ✨ Возможности

### Основные функции
✅ **Загрузка всех чатов** - автоматическое получение списка всех ваших чатов из Telegram

✅ **Красивый интерфейс** - эмодзи, понятные сообщения, удобная навигация

✅ **Отправка массовых сообщений** - одновременная отправка в выбранный чат

✅ **Безопасная отправка** - система повторных попыток при сбоях

✅ **Обработка ошибок** - FloodWait, RPC ошибки, таймауты

✅ **Логирование** - детальная информация о процессе отправки

✅ **Интерактивное меню** - простой выбор чатов и параметров

✅ **Валидация данных** - проверка файлов и сообщений перед отправкой

### Дополнительные возможности
🔄 **Повторные попытки** - автоматическое восстановление при ошибках подключения

⏳ **Обработка Flood Wait** - автоматическое ожидание при ограничениях Telegram

📊 **Статистика отправки** - детальный отчет о результатах

🛡️ **Обработка исключений** - корректное завершение при критических ошибках

💾 **Поддержка UTF-8** - полная поддержка русского и других языков

🔌 **Переподключение** - автоматическое переподключение при разрыве связи

---

## 🔧 Требования

### Обязательные
- **Python** 3.8 или выше ([скачать](https://www.python.org/downloads/))
- **pip** - менеджер пакетов Python (обычно идет с Python)
- **macOS / Linux / Windows** - любая операционная система
- **Интернет соединение** - для работы с Telegram API

### Рекомендуемые
- **VS Code** - для редактирования кода
- **Terminal / CMD** - для запуска приложения
- **Git** - для клонирования репозитория (опционально)

### Python зависимости
```
telethon >= 1.26.0   # Telegram клиент для Python
asyncio              # (встроенная библиотека Python)
logging              # (встроенная библиотека Python)
os, re               # (встроенные библиотеки Python)
```

---

## 📦 Установка

### Вариант 1: Быстрая установка (Рекомендуется)

#### macOS / Linux

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/alezhg1/Telethon-Telegram-message-sender-Telethon-
cd telegram-message-sender

# 2. Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# 3. Установите зависимости
pip install -r requirements.txt

# 4. Переходите к конфигурации
```

#### Windows (CMD)

```cmd
REM 1. Клонируйте репозиторий
git clone https://github.com/yourusername/telegram-message-sender.git
cd telegram-message-sender

REM 2. Создайте виртуальное окружение
python -m venv venv
venv\Scripts\activate

REM 3. Установите зависимости
pip install -r requirements.txt

REM 4. Переходите к конфигурации
```

#### Windows (PowerShell)

```powershell
# 1. Клонируйте репозиторий
git clone https://github.com/yourusername/telegram-message-sender.git
cd telegram-message-sender

# 2. Создайте виртуальное окружение
python -m venv venv
.\venv\Scripts\Activate.ps1

# 3. Установите зависимости
pip install -r requirements.txt

# 4. Переходите к конфигурации
```

### Вариант 2: Ручная установка

```bash
# Без виртуального окружения (не рекомендуется)
pip install telethon
```

### Проверка установки

```bash
# Проверьте версию Python
python3 --version
# Должно вывести: Python 3.8.0 или выше

# Проверьте установку Telethon
python3 -c "import telethon; print(telethon.__version__)"
# Должно вывести версию, например: 1.26.0
```

---

## ⚙️ Конфигурация

### Шаг 1: Создание файла config.py

Создайте файл `config.py` в корне проекта:

```python
# config.py

# Получить ID и HASH на https://my.telegram.org/
API_ID = 'xxxxxxxx'              #брать на https://my.telegram.org
API_HASH = 'xxxxxx' #брать на https://my.telegram.org

PHONE_NUMBER = '+xxxxxxxxxxx' # Ваш номер телефона

# Имя файла сессии (создается автоматически)
SESSION_FILE = 'sender_bot.session'
```

### Шаг 2: Получение API ID и API HASH

1. Откройте https://my.telegram.org/
2. Введите ваш номер телефона
3. Введите код подтверждения
4. Перейдите в **API development tools**
5. Нажмите **Create new application**
6. Заполните форму:
   - **App title**: любое имя (например "Telegram Sender")
   - **Short name**: английское имя (например "tg_sender")
   - Остальное оставьте по умолчанию
7. Получите **api_id** и **api_hash**
8. Скопируйте их в `config.py`

### Шаг 3: Защита конфигурации

**⚠️ ВАЖНО:** Не коммитьте `config.py` в Git!

Добавьте в `.gitignore`:

```bash
echo "config.py" >> .gitignore
echo "sender_bot.session" >> .gitignore
echo "*.session" >> .gitignore
echo "venv/" >> .gitignore
echo "__pycache__/" >> .gitignore
echo "*.pyc" >> .gitignore
```

---

## 🚀 Использование

### Запуск приложения

#### macOS / Linux
```bash
# Активируйте окружение (если еще не активировано)
source venv/bin/activate

# Запустите программу
python3 telegram_clean.py
```

#### Windows
```cmd
# Активируйте окружение
venv\Scripts\activate

# Запустите программу
python telegram_clean.py
```

### Первый запуск

При первом запуске программа попросит авторизацию:

```
🔌 Подключаемся к Telegram...
✅ Клиент запущен успешно!
🤖 ЗАПУСК ТЕЛЕГРАМ ОТПРАВИТЕЛЯ
==================================================

Пожалуйста, введите код подтверждения:
```

1. Откройте Telegram на вашем телефоне
2. Вы получите код подтверждения
3. Введите код в консоль
4. Нажмите Enter

**Файл сессии `sender_bot.session` будет сохранен автоматически.**

### Основной интерфейс

```
🤖 ЗАПУСК ТЕЛЕГРАМ ОТПРАВИТЕЛЯ
==================================================
🔌 Подключаемся к Telegram...
✅ Клиент запущен успешно!
🔌 Соединение разорвано, переподключаемся...
✅ Загружено чатов: 47

============================================================
📋 СПИСОК ВАШИХ ЧАТОВ:
============================================================
  1. 👤 John Doe
  2. 📢 Tech News Channel
  3. 👥 Python Developers Group
  4. 💬 Personal Chat
 ...

🎯 Введите номер чата для отправки: 
```

### Пошаговый процесс отправки

#### Шаг 1: Выбор чата
```
🎯 Введите номер чата для отправки: 2
✅ Выбран чат: Tech News Channel
```

#### Шаг 2: Выбор файла
```
📁 Введите путь к файлу .txt: messages.txt
✅ Файл найден: messages.txt (456 байт)
```

#### Шаг 3: Просмотр содержимого
```
📨 Прочитано сообщений: 5

📋 Первые 5 сообщений:
----------------------------------------
1. Привет! Как дела?
2. Проверка сообщения №2
3. Это третье сообщение
4. Четвертое сообщение
5. И последнее пятое!
----------------------------------------
```

#### Шаг 4: Подтверждение
```
============================================================
🚀 ПОДТВЕРЖДЕНИЕ ОТПРАВКИ
============================================================
💬 Чат: Tech News Channel
📨 Сообщений: 5
============================================================

Отправить сообщения? (да/нет): да
```

#### Шаг 5: Отправка с прогрессом
```
📤 Начинаем отправку в чат: Tech News Channel
⏳ Это может занять некоторое время...
📨 Отправлено 1/5 сообщений
📨 Отправлено 2/5 сообщений
📨 Отправлено 3/5 сообщений
📨 Отправлено 4/5 сообщений
📨 Отправлено 5/5 сообщений
```

#### Шаг 6: Результаты
```
============================================================
📊 ИТОГИ ОТПРАВКИ:
============================================================
✅ Успешно отправлено: 5
❌ Не отправлено: 0
💬 Чат: Tech News Channel
============================================================

🎉 Работа завершена!
```

---

## 📁 Структура проекта

```
telegram-message-sender/
│
├── 📄 telegram_clean.py          # Главный файл приложения
├── 📄 config.py                  # Конфигурация (не коммитить!)
├── 📄 README.md                  # Этот файл
├── 📄 LICENSE                    # MIT лицензия
├── 📄 requirements.txt           # Python зависимости
├── 📄 .gitignore                 # Git исключения
│
├── 📁 venv/                      # Виртуальное окружение (не коммитить!)
│   ├── bin/                      # Бинарники Python (macOS/Linux)
│   ├── Scripts/                  # Бинарники Python (Windows)
│   └── lib/                      # Установленные пакеты
│
├── 📁 examples/                  # Примеры использования
│   ├── messages.txt              # Пример файла с сообщениями
│   ├── long_messages.txt         # Длинные сообщения
│   └── multi_line.txt            # Многострочные сообщения
│
├── 📁 docs/                      # Документация
│   ├── SETUP.md                  # Подробная установка
│   ├── TROUBLESHOOTING.md        # Решение проблем
│   ├── API.md                    # Описание API
│   └── EXAMPLES.md               # Примеры кода
│
└── 📁 logs/                      # Логи приложения (создается автоматически)
    └── sender_bot.log            # Лог файл
```

---

## 🔌 API и классы

### Главный класс: TelegramSender

```python
from telegram_clean import TelegramSender, TelegramClient
from config import API_ID, API_HASH, PHONE_NUMBER, SESSION_FILE

# Создание клиента
client = TelegramClient(SESSION_FILE, API_ID, API_HASH)

# Создание отправителя
sender = TelegramSender(client)
```

### Основные методы

#### get_all_chats()
Получает список всех чатов пользователя.

```python
async def get_all_chats(self) -> list:
    """
    Возвращает список всех диалогов (чатов, групп, каналов)
    
    Returns:
        list: Список диалогов с сущностями
        
    Example:
        dialogs = await sender.get_all_chats()
        for dialog in dialogs:
            print(f"{dialog.entity.title}: {dialog.unread_count} новых")
    """
```

#### display_chats()
Выводит красивый список чатов в консоль.

```python
def display_chats(self, dialogs: list) -> None:
    """
    Отображает список чатов с номерами и типами
    
    Args:
        dialogs (list): Список диалогов от get_all_chats()
        
    Output:
        📋 СПИСОК ВАШИХ ЧАТОВ:
        1. 👤 John Doe
        2. 📢 News Channel
    """
```

#### safe_send_message()
Безопасно отправляет одно сообщение с повторными попытками.

```python
async def safe_send_message(
    self, 
    chat, 
    message: str, 
    max_retries: int = 3
) -> bool:
    """
    Отправляет сообщение с обработкой ошибок
    
    Args:
        chat: Объект чата
        message (str): Текст сообщения
        max_retries (int): Макс. количество попыток
        
    Returns:
        bool: True если успешно, False иначе
        
    Handles:
        - FloodWaitError: автоматическое ожидание
        - RPCError: повторная попытка после задержки
        - ConnectionError: переподключение
    """
```

#### send_messages()
Отправляет все сообщения в выбранный чат.

```python
async def send_messages(
    self, 
    chat, 
    messages: list
) -> dict:
    """
    Отправляет несколько сообщений в чат
    
    Args:
        chat: Объект чата
        messages (list): Список текстов сообщений
        
    Returns:
        dict: {
            'sent': 5,      # Успешно отправлено
            'failed': 0     # Не отправлено
        }
    """
```

#### read_messages_from_file()
Читает сообщения из текстового файла.

```python
def read_messages_from_file(self, file_path: str) -> list:
    """
    Читает сообщения из файла (одно на строку)
    
    Args:
        file_path (str): Путь к файлу .txt
        
    Returns:
        list: Список сообщений (без пустых строк)
        
    Raises:
        UnicodeDecodeError: Если файл не UTF-8
        FileNotFoundError: Если файл не найден
        
    Example:
        messages = sender.read_messages_from_file('messages.txt')
        # ['Message 1', 'Message 2', 'Message 3']
    """
```

#### run()
Главный метод с интерактивным интерфейсом.

```python
async def run(self) -> None:
    """
    Запускает интерактивное меню приложения
    - Загрузка чатов
    - Выбор чата
    - Выбор файла
    - Отправка сообщений
    """
```

---

## 📚 Примеры использования

### Пример 1: Отправка простых сообщений

**Файл: examples/messages.txt**
```
Привет! Как дела?
Это сообщение номер 2
Третье сообщение в очереди
Четвертое сообщение
И последнее сообщение!
```

**Запуск:**
```bash
python3 telegram_clean.py

# Выберите чат, укажите messages.txt, подтвердите
# Готово! Все 5 сообщений отправлены
```

### Пример 2: Отправка многострочных сообщений

**Файл: examples/multi_line.txt**
```
Первая строка
Вторая строка
Третья строка

Это второе сообщение
с несколькими строками
и пустой строкой между
```

⚠️ **Важно:** Пустые строки создают разделение между сообщениями. Каждое непустое сообщение отправляется отдельно.

### Пример 3: Отправка с форматированием

**Файл: examples/formatted.txt**
```
*Жирный текст*
_Курсивный текст_
__Подчеркнутый__
~Зачеркнутый~

```markdown
# Заголовок
Это код на Python
```
```

### Пример 4: Программный запуск

```python
import asyncio
from telegram_clean import TelegramSender, TelegramClient
from config import API_ID, API_HASH, PHONE_NUMBER, SESSION_FILE

async def main():
    # Создание клиента
    client = TelegramClient(SESSION_FILE, API_ID, API_HASH)
    
    # Подключение
    await client.start(phone=PHONE_NUMBER)
    
    # Создание отправителя
    sender = TelegramSender(client)
    
    # Получение чатов
    dialogs = await sender.get_all_chats()
    
    # Отправка в первый чат
    if dialogs:
        messages = ["Сообщение 1", "Сообщение 2"]
        result = await sender.send_messages(dialogs[0], messages)
        print(f"Отправлено: {result['sent']}, Ошибок: {result['failed']}")
    
    # Отключение
    await client.disconnect()

# Запуск
asyncio.run(main())
```

### Пример 5: С обработкой ошибок

```python
async def safe_broadcast(chat_name, messages):
    try:
        client = TelegramClient(SESSION_FILE, API_ID, API_HASH)
        await client.start(phone=PHONE_NUMBER)
        
        sender = TelegramSender(client)
        dialogs = await sender.get_all_chats()
        
        # Найти чат по имени
        target_chat = None
        for dialog in dialogs:
            if chat_name in str(dialog.entity.title):
                target_chat = dialog
                break
        
        if target_chat:
            result = await sender.send_messages(target_chat, messages)
            print(f"✅ Успешно отправлено: {result['sent']}")
        else:
            print(f"❌ Чат '{chat_name}' не найден")
            
    except Exception as e:
        print(f"❌ Ошибка: {e}")
    finally:
        await client.disconnect()

# Использование
await safe_broadcast("My Channel", ["Test message 1", "Test message 2"])
```

---

## ⚠️ Обработка ошибок

### FloodWaitError (Ограничение Telegram)

**Проблема:** Слишком много сообщений отправлено за короткое время

**Решение:** Встроенная система автоматически ждет нужное время

```python
# В коде уже реализовано:
except FloodWaitError as e:
    wait_time = e.seconds
    print(f"⏳ Flood wait: ждем {wait_time} секунд...")
    await asyncio.sleep(wait_time + 1)
```

**Как избежать:**
- Увеличьте задержку между сообщениями
- Редактируйте строку в коде:
```python
await asyncio.sleep(0.5)  # Измените на 1, 2, 5 или больше
```

### RPC Error (Ошибка Telegram API)

**Проблема:** Telegram вернул ошибку

**Решение:** Автоматическая повторная попытка (до 3 раз)

```python
except RPCError as e:
    print(f"❌ Ошибка RPC: {e}")
    # Программа автоматически повторит попытку
```

### ConnectionError (Разрыв соединения)

**Проблема:** Потеряна связь с интернетом или Telegram

**Решение:** Автоматическое переподключение

```python
if not self.client.is_connected():
    print("🔌 Соединение разорвано, переподключаемся...")
    await self.client.connect()
```

### File Not Found (Файл не найден)

**Проблема:** Указан неправильный путь к файлу

**Решение:** Проверьте:
1. Правильно ли написан путь
2. Файл находится в той же папке что и скрипт
3. Файл с расширением .txt

```bash
# Правильно:
/Users/artem/vs_code/messages.txt
messages.txt (если в той же папке)
./messages.txt

# Неправильно:
messages
/messages.txt (на Mac)
C:\messages.txt (если на Windows используйте forward slash)
```

### UnicodeDecodeError (Кодировка файла)

**Проблема:** Файл не в кодировке UTF-8

**Решение:** 
1. Откройте файл в VS Code
2. Нажмите правой кнопкой на кодировку справа внизу (например "UTF-8")
3. Выберите "Save with Encoding" → "UTF-8"

---

## 💡 Советы

### 1. Сохранение логов в файл

Добавьте в начало файла:

```python
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('telegram_sender.log'),
        logging.StreamHandler()
    ]
)
```

### 2. Использование прокси/VPN

Если Telegram заблокирован, используйте MTProto прокси:

```python
from telethon.network import ConnectionTcpMTProxyAbridged

client = TelegramClient(
    SESSION_FILE,
    API_ID,
    API_HASH,
    proxy=(ConnectionTcpMTProxyAbridged, 'proxy.example.com', 443, 'secret') #используйте ВАШ proxy-сервер
)
```

---

## 🔒 Безопасность

### ⚠️  Меры безопасности

1. **Никогда не коммитьте config.py**
   ```bash
   echo "config.py" >> .gitignore
   ```

2. **Никогда не делитесь SESSION_FILE**
   - Файл содержит авторизацию
   - Любой может получить доступ к вашему аккаунту
   ```bash
   echo "*.session" >> .gitignore
   ```

3. **Используйте сильные пароли Telegram**
   - Не используйте для критичных аккаунтов
   - Рекомендуется создать отдельный аккаунт

4. **Скрывайте sensitive информацию**
   ```python
   # НЕПРАВИЛЬНО
   API_HASH = '1b4j5h7s56'  # Видно в коде
   
   # ПРАВИЛЬНО
   import os
   API_HASH = os.environ.get('TELEGRAM_API_HASH')
   ```

5. **Регулярно обновляйте зависимости**
   ```bash
   pip install --upgrade telethon
   ```

---

## 🐛 Решение частых проблем

### Проблема: "ModuleNotFoundError: No module named 'telethon'"

**Решение:**
```bash
# Убедитесь, что окружение активировано
source venv/bin/activate  # macOS/Linux
venv\Scripts\activate     # Windows

# Установите телетон
pip install telethon
```

### Проблема: "FileNotFoundError: sender_bot.session"

**Решение:** Это нормально при первом запуске. Файл будет создан после авторизации.

### Проблема: "Invalid phone number"

**Решение:** Используйте формат +7 (Россия) или +1 (США):
```python
PHONE_NUMBER = '+79151234567'  # Правильно
PHONE_NUMBER = '9151234567'    # Неправильно
```

### Проблема: Telegram требует код подтверждения каждый раз

**Решение:** Проверьте, что `sender_bot.session` не удаляется. Сессия должна сохраняться.

### Проблема: "Error in RpcRequest: PEER_ID_INVALID"

**Решение:** Вы выбрали неправильный чат. Убедитесь, что чат все еще существует и вы в нем участник.

### Проблема: Программа виснет при отправке

**Решение:** Возможно Telegram вернул FloodWait. Подождите или увеличьте задержку:
```python
await asyncio.sleep(5)  # Вместо 0.5
```

---

## 📝 Логирование

Приложение автоматически логирует все события:

```
2025-01-15 14:30:45 - INFO - 🤖 ЗАПУСК ТЕЛЕГРАМ ОТПРАВИТЕЛЯ
2025-01-15 14:30:46 - INFO - ✅ Клиент запущен успешно!
2025-01-15 14:30:48 - INFO - ✅ Загружено чатов: 47
2025-01-15 14:30:52 - INFO - ✅ Выбран чат: My Channel
2025-01-15 14:30:54 - INFO - 📨 Прочитано сообщений: 5
2025-01-15 14:31:00 - INFO - ✅ Успешно отправлено: 5
```

Логи хранятся в консоли. Для сохранения в файл см. раздел "Советы и трюки".

---

## 📞 Поддержка и контакты

### Если возникла проблема

1. **Проверьте документацию**
   - Раздел "Решение частых проблем"
   - Раздел "Обработка ошибок"

2. **Посмотрите примеры**
   - Папка `examples/`
   - Раздел "Примеры использования"

3. **Включите подробные логи**
   - Измените logging level на DEBUG
   - Поищите ошибку в логах

4. **Откройте Issue на GitHub**
   - Опишите проблему
   - Прикрепите логи (без чувствительной информации!)
   - Укажите версию Python и Telethon

### Полезные ссылки

- 🔗 [Telethon документация](https://docs.telethon.dev/)
- 🔗 [Получение API_ID и API_HASH](https://my.telegram.org/) - для получения API ключей
- 🔗 [Python документация](https://docs.python.org/3/)

---

## 📄 Лицензия

Этот проект лицензирован под **MIT License** - см. файл [LICENSE](LICENSE) для деталей.

---

## ⚠️ Дисклеймер

**Этот проект создан в образовательных целях.**

- Используйте инструмент ответственно
- Уважайте условия обслуживания Telegram
- Не используйте для спама или нарушения прав
- Автор не несет ответственность за неправомерное использование
- Использование данного проекта в целях спама может привести к блокировке аккаунта Telegram

**Ваша ответственность использовать это ПО только в легальных и этичных целях!**

---

## 🎉 Готово!

Спасибо за использование **Telegram Message Sender**! 

Если этот проект помог вам, пожалуйста:
- ⭐ Поставьте звездочку репозиторию
- 🍴 Сделайте Fork и предложите улучшение 
- 📣 Поделитесь с друзьями

---

## 📝 Версия и история изменений

**Текущая версия:** 1.0.0

### v1.0.0 (2025-01-15)
- ✅ Первоначальный релиз
- ✅ Загрузка чатов
- ✅ Отправка массовых сообщений
- ✅ Обработка ошибок
- ✅ Интерактивный интерфейс
- ✅ Полная документация

---

**Автор:** alez
**Контакт:** t.me/newzellandtg 
**GitHub:** https://github.com/alezhg1  
**Последнее обновление:** 23 ноября 2025

---

Made with ❤️ for Telegram users
